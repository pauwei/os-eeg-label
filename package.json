{
  "name": "os-eeg-label",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
    "install": "concurrently \"cd backend && npm i\" \"cd frontend && npm i\" ",
    "start": " cd backend && node index.js",
    "heroku-postbuild": "cd backend && npm install && cd ../frontend && npm install --legacy-peer-deps && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pauwei/os-eeg-label.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/pauwei/os-eeg-label/issues"
  },
  "homepage": "https://github.com/pauwei/os-eeg-label#readme",
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}
